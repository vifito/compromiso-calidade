{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edición Pregunta</h1>

    {{ form_start(edit_form) }}

    <div class="form-group">
        {{ form_label(edit_form.texto) }}
        {{ form_widget(edit_form.texto, { 'attr': {'class': 'form-control'} }) }}
    </div>

    <div class="form-group">
        {{ form_label(edit_form.orde) }}
        {{ form_widget(edit_form.orde, { 'attr': {'class': 'form-control'} }) }}
    </div>


    <input type="submit" value="Editar" class="btn btn-warning"/>
    {{ form_end(edit_form) }}

    <div class="pull-right">
        <a href="{{ path('pregunta_index') }}" class="btn btn-primary">Listado</a>

        {{ form_start(delete_form) }}
        <input type="submit" value="Eliminar" class="btn btn-danger"/>
        {{ form_end(delete_form) }}
    </div>
{% endblock %}
