{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nova Pregunta</h1>

    {{ form_start(form) }}



    <div class="form-group">
        {{ form_label(form.texto) }}
        {{ form_widget(form.texto, { 'attr': {'class': 'form-control'} }) }}
    </div>

    <div class="form-group">
        {{ form_label(form.orde) }}
        {{ form_widget(form.orde, { 'attr': {'class': 'form-control'} }) }}
    </div>

        <input type="submit" value="Crear" class="btn btn-success" />
    {{ form_end(form) }}

    <div class="pull-right">
            <a href="{{ path('pregunta_index') }}" class="btn btn-primary">Listado</a>
        </div>
{% endblock %}
