{% extends 'base.html.twig' %}

{% block body %}
    <h1>Pregunta list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Texto</th>
                <th>Orde</th>
                <th>Valores</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for preguntum in preguntas %}
            <tr>
                <td>
                    <a href="{{ path('pregunta_show', { 'id': preguntum.id }) }}" title="{{ preguntum.id }}">
                        {{ preguntum.texto }}
                    </a>
                </td>
                <td>{{ preguntum.orde }}</td>
                <td>
                    <select>
                    {% for opcion in preguntum.opcions %}
                        <option>{{ opcion.valor }}</option>
                    {% endfor %}
                    </select>
                </td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('pregunta_show', { 'id': preguntum.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('pregunta_edit', { 'id': preguntum.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('pregunta_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
